# Gemini (server only). GEMINI_MODEL defaults to gemini-3.8-flash.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.8-flash

# Upstash Redis (added by the Vercel integration). Either pair works with Redis.fromEnv().
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
